[↑ Up to Section](main.md)

[↑↑ Back to Main Documentation](../../main.md)

# 2.33.7
DWDT_NS_RegisterCallback Function

_Generated on: 2025-02-19 08:42:42_

_Source: https://onlinedocs.microchip.com/oxy/GUID-450989FA-38E4-4D68-AB61-15ADB29AD718-en-US-5/GUID-1C148CED-AEDC-474E-AA61-E1D3DE5E76D0.html_

# 2.33.7 DWDT_NS_RegisterCallback Function

## C

```C
void DWDT_NS_RegisterCallback(DWDT_CALLBACK pCallback, void* pContext);
```

## Summary

Allows application to register callback with PLIB

## Description

This function allows application to register a callback function for the
PLIB to call back when never secure interrupt occurred.

At any point if application wants to stop the callback, it can call this
function with "callback" value as NULL.

## Precondition

The DWDT_Initialize function must have been called and one of the never secure interrupts is enabled.

## Parameters

### Parameters

| Parameter | Description |
|-----------|-------------|
| callback | Pointer to the callback function implemented by the user. |
| pContext | The value of parameter will be passed back to the application unchanged, when the callback function is called. It can be used to identify any application specific value that identifies the instance of the client module (for example, it may be a pointer to the client module's state structure). |

| Param | Description |
| --- | --- |
| callback | Pointer to the callback function implemented by the user. |
| pContext | The value of parameter will be passed back to the application unchanged, when the callback function is called. It can be used to identify any application specific value that identifies the instance of the client module (for example, it may be a pointer to the client module's state structure). |

## Returns

None.

## Example

```C
MY_APP_OBJ myAppObj;

void APP_DWDT_CallbackFunction (bool interruptStatus, void* pContext)
{
    // The context was set to an application specific object.
    // It is now retrievable easily in Callback function.
    MY_APP_OBJ myAppObj = (MY_APP_OBJ *) context;
    //Application related tasks
}

DWDT_NS_RegisterCallback (APP_DWDT_CallbackFunction, &myAppObj);
```

## Remarks

None.